{
  "$schema": "https://deno.land/x/denon/schema.json",
  "scripts": {
    "start": "deno run --import-map=import_map.json --config ./deno.json --allow-net --allow-read --allow-env --unstable --no-check server/server.ts"
  }
}
